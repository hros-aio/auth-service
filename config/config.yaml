app:
  name: "hrms-auth-service"
  port: 3000
  env: "development"

database:
  host: "localhost"
  port: 5432
  username: "postgres"
  password: "postgres"
  name: "auth"

redis:
  host: "localhost"
  port: 6379

kafka:
  brokers:
    - "localhost:9092"

jwt:
  publicKey: "test-public-key"
  privateKey: "test-private-key"
