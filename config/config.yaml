app:
  name: "hrms-access-service"
  port: 3000
  env: "development"

database:
  host: "localhost"
  port: 5432
  username: "hrms_user"
  password: "hrms_password"
  name: "hrms_access_db"

redis:
  host: "localhost"
  port: 6379

kafka:
  brokers:
    - "localhost:9092"

jwt:
  publicKey: "test-public-key"
  # privateKey is intentionally omitted; load via JWT_PRIVATE_KEY environment variable or secrets manager
