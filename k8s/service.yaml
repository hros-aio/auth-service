apiVersion: v1
kind: Service
metadata:
  name: hrms-access-service
  namespace: hrms
  labels:
    app: hrms-access-service
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: hrms-access-service
  type: ClusterIP
